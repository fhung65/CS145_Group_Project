import sklearn.LinearRegression

model = sklearn.LinearRegression()

A,b = import_data_function()
model.predict(A,b)

x, ground_truth = import_test_sample()
prediction = model.predict(A, x)

mean_squared_error = np.linalg.norm(x - ground_truth)**2
